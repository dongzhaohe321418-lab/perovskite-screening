# 下一阶段研究指引 — FA₀.₉₅Cs₀.₀₅PbI₃ 转向(给 Mac mini 的 claudescience)

写于 2026-07-23(MacBook Pro 会话)。前提:已读 GitHub 上全部 14 个新 commit 的
报告。**战略决定:生产筛选宿主改为 FA₀.₉₅Cs₀.₀₅PbI₃(实用组分);CsPbI₃ 降级为
方法学验证平台,已有成果全部保值。** proposal 已更新到 rev.3(见 proposal/ 目录,
新增 §6 Preliminary Results,就是你们的数据)。

## 总原则(适用于以下所有任务)

1. **FA 宿主的每个垒都是分布,不是数字**:报 N、均值、标准差、极差。单构型数字
   一律标注 "single-config, not rankable"(GA 教训的制度化);
2. **诚实状态标签制度延续**:sanity ✓ / trend ✓ / validated 三档,宁降不升;
3. **回归测试不许破**:γ-CsPbI₃ 0.259 eV 在任何环境变更后必须复现;FA 基线稳定后
   也建一条 FA 回归路径;
4. **花钱纪律**:E-HPC 批次结束节省停机;AutoDL 跑完关机;每份报告末尾记录本批
   实际花费;
5. 每个任务产出:`results/<track>/` 下的 JSON 原始数据 + REPORT_*.md + 可复现
   命令行,commit 并 push。

---

## Track A — CsPbI₃ 方法学线(继续,不受转向影响)

### A1. 带电弛豫路径(最高优先级,anchor b 的真正完成)⭐
DFT_BENCHMARK 已证明固定几何单点只给 0.90 比值;Tyagi 分离是结构弛豫效应。
- E-HPC 上对 159 原子胞做 **q=+1 的几何弛豫**:两个端点 + MACE 鞍点附近 3 张图
  (`tot_charge=1`,离子弛豫,力收敛 ~1e-3 Ry/Bohr;报告估计每个端点 ~5 h/32核);
- 产出:弛豫后的 V_I⁺ 垒 vs V_I⁰ 垒;与 Tyagi 2025 的分离方向/量级对比;
- **弛豫轨迹的每一步 SCF 都存下来**——这就是逐电荷态微调训练集 v0 的种子数据;
- 验收:能回答"带电弛豫是否把分离拉向 Tyagi 方向"。无论答案是否,都写报告。

### A2. QE 生产设置收敛(为微调数据定标准)
- ecutwfc {40,50,60,70} Ry × k {Γ, 2×2×2} 的垒收敛矩阵(单点即可,8–10 个 SCF);
- 选定生产设置后,所有微调训练数据统一用它;
- 验收:垒对 cutoff/k 的收敛曲线,选定点误差 <10 meV。

### A3. 微调 v0(A1+A2 之后)
- 训练集:A1 的带电弛豫构型 + 现有 8 个单点 + 路径附近扰动构型(每构型摄动
  0.05–0.1 Å,~50–100 个,E-HPC 批量单点);
- 每电荷态一个 MACE 微调(5090 上训练,一轮几小时);
- 验收:held-out γ 路径上 |Ea^MLIP − Ea^DFT| < 0.05 eV。达标后才有资格排名。

## Track B — FA 宿主启用线(新,与 Track A 并行)

### B1. α-FAPbI₃ 建胞 + 取向系综协议
- 12 原子赝立方胞(FA 手工/SMILES 构型),MACE 弛豫;
- 2×2×2(96 原子)MLIP-MD:300 K,NVT,20–50 ps,每 2–5 ps 抽一帧去相关快照,
  淬火弛豫 → **8 个取向构型**的系综;
- 注意:zero-shot MACE 对 FA 转动的描述质量未知,MD 只用来生成合理取向样本,
  不做动力学结论;
- 验收:8 个构型的能量分布 + 结构合理性检查(Pb–I 骨架完好、FA 未解离)。

### B2. FA 宿主 V_I 基线分布(FA 侧的"曳光弹")⭐
- B1 的 8 个构型各跑同一条八面体棱边 V_I hop 的 CI-NEB(5090,float64);
- 产出:**Ea 分布(均值±标准差)**——这个散布宽度决定后面所有 FA 任务的采样预算,
  是本阶段最重要的单个数字;
- 与文献 FA 基体系区间(器件级 ~0.4–0.6 eV,计算值更宽)做 sanity 对照;
- 验收:分布报告 + 新的 FA 回归基线路径入库。

### B3. 2×2×5 组分胞 + "Cs 本身是不是钉扎剂"⭐(第一个 FA 科学问题)
- 2×2×5 超胞(240 原子,20 个 A 位)放 1 个 Cs = 恰好 x=0.05;
- Cs 近路径 / 远路径两种摆位 × ≥4 个 FA 取向 → ΔEa(Cs) 分布;
- 这直接回答"5% Cs 只稳相还是也钉扎碘迁移"——无论结果如何都可发表,而且
  解释实用配方;
- 验收:ΔEa(Cs) 均值±散布 + 机制指纹(Cs 局部几何 vs FA 氢键网络变化)。

### B4. QE 的 FA 侧就绪
- 补 H/C/N 赝势(与现有 pslibrary/SSSP 一致的家族);
- **加 D3 色散**(`vdw_corr='grimme-d3'`)——有机阳离子体系必须;CsPbI₃ 侧
  同步加 D3 重跑一个对照单点,量化 D3 对垒的影响;
- FAPbI₃ 96 原子胞单点计时(32 核预计 <1 h);
- 验收:FA 胞 SCF 收敛 + 计时数据进成本模型。

### B5. 转移性探针(A3 + B2 之后)
- 把 CsPbI₃ 初筛的代表(Sr@Pb、Br@I)+ GA@A位 在 FA 宿主重算(微调模型);
- 产出:CsPbI₃→FA 排名相关性,直接写进 proposal 的 transferability deliverable;
- 验收:相关系数 + "两级漏斗是否成立"的判断。

## 优先级与依赖

```
A1 ⭐ ──→ A3 ──→ B5
A2 ──↗          ↗
B1 ──→ B2 ⭐ ──→ B3 ⭐
B4(独立,随时可做)
```

建议本周:A1 + B1/B2 并行(分别吃 E-HPC 和 5090,互不抢资源),B4 见缝插针。
γ 相重跑立方初筛的旧计划**取消**——被 FA 转向取代,不要花算力。

## 成本预估

- Track A:A1 ~10–15 节点小时,A2 ~5,A3 训练在 5090 上忽略不计 → **E-HPC
  约 ¥300–500**;
- Track B:B1+B2 在 5090 上 <1 天(几十块钱);B3 约 2×;B4 的 QE 部分 ~¥100;
- 全部加起来 <¥1500,在预算内。超预算 50% 时停下来汇报。

## 汇报节点

每完成一个 ⭐ 任务,更新 HANDOFF.md 的进度节 + push,并在报告里明确回答:
- A1:带电弛豫是否拉开 V_I⁺/V_I⁰ 分离?
- B2:FA 宿主的取向散布多宽?(决定采样预算)
- B3:Cs 是否钉扎?

proposal rev.3 的 tex/pdf 在 proposal/ 目录,请连同本文件一起 commit 进仓库。
